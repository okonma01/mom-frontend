{
    "builds": [
        { "src": "server/app.py", "use": "@vercel/python" },
        { "src": "server/**/*.py", "use": "@vercel/python" },
        { "src": "server/**/*.!(py)", "use": "@vercel/static" }
    ],
    "routes": [
        { "src": "/(.*)", "dest": "server/app.py" }
    ]
}